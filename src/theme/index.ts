/**
 * Global design system for SareeLoom.
 * Import from here (or from `src/constant` for legacy aliases).
 *
 * Dark mode: not enabled in the app today. Tokens are light-mode only;
 * introduce a ThemeProvider + dark palette if needed later.
 */

export { colors, color } from './colors';
export type { ColorKey } from './colors';
export { fonts, fontSize, typography } from './typography';
export { spacing, radius, layout } from './spacing';
export { shadows } from './shadows';
export { commonStyles } from './commonStyles';
